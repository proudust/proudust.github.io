{
  "name": "proudust.github.io",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=front",
    "clean": "npm run clean --workspace=front",
    "deploy": "npm run deploy --workspace=front",
    "fix": "run-p fix:*",
    "fix:front": "npm run fix --workspace=front",
    "fix:posts": "npm run fix --workspace=posts",
    "lint": "run-p lint:*",
    "lint:front": "npm run lint --workspace=front",
    "lint:posts": "npm run lint --workspace=posts",
    "start": "npm run start --workspace=front"
  },
  "workspaces": [
    "packages/*"
  ]
}
