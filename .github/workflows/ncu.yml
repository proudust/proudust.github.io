name: ncu

on:
  repository_dispatch:
    types: [ncu]
  schedule:
    - cron: "0 21 * * *"
  workflow_dispatch:

jobs:
  ncu:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ssh-key: ${{ secrets.ACTIONS_DEPLOY_KEY }}

      - name: Use yarn cache
        uses: actions/cache@v2
        with:
          path: ~/.cache/yarn
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
            ${{ runner.os }}-

      - name: Run npm-check-updates and yarn-deduplicate
        run: |
          yarn global add npm-check-updates yarn-deduplicate
          `yarn global bin`/ncu -u --packageFile packages/front/package.json
          `yarn global bin`/ncu -u --packageFile packages/posts/package.json
          yarn
          `yarn global bin`/yarn-deduplicate yarn.lock

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v2
        with:
          branch: update-deps
          commit-message: ":arrow_up: Update dependencies"
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          title: Update dependencies
          body: |
            This pull request is automatically generated by GitHub Actions workflow.
            Dependencies update by [raineorshine/npm-check-updates](https://github.com/raineorshine/npm-check-updates) every morning at 6am (JST).
          labels: automated pr
          assignees: proudust
          reviewers: proudust
